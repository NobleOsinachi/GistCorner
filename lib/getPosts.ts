// // import { Post } from "@/app/api/data/route";

// export default async function getPost(id?: number) {
//   const data = await fetch("/api/posts" + id ? "/" + id : "/");
//   const posts: Array<Post> | Post = await data.json();
//   return posts;
// }
